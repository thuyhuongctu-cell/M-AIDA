# Secrets - never commit real credentials
.env
backend/.env
*.env
!.env.example
!backend/.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
/coverage
/build
.pytest_cache/
*.egg-info/

# SQLite study store (runtime data, never source)
*.db
*.db-wal
*.db-shm

# Vite production output
frontend/build/

# editor / script backups are never part of the registered work
*.bak
